# ============================================================
# 只提交源代码与文档；一切构建产物、依赖、运行期数据都不入库
# ============================================================

# ---------- Rust / Cargo 构建产物 ----------
/target/
**/target/
**/*.rs.bk
*.pdb
*.ilk
*.exp

# ---------- Node / 前端依赖与产物 ----------
node_modules/
**/node_modules/
dist/
**/dist/
dist-ssr/
# `vite build --outDir dist-verify` 的产物：沙箱的批量删除保护会拦住 Vite
# 清空 dist 的动作（见项目记忆第 8 条），所以验证打包时改用这个全新目录。
dist-verify/
**/dist-verify/
# Vite 读配置时写的临时文件（`vite.config.ts.timestamp-*.mjs`）。
# 构建**失败**或被中断时不会自己清掉，于是每次 `git status` 都冒出来一批。
vite.config.ts.timestamp-*

# ---------- 沙箱改写受阻时的过渡目录 ----------
# 助手在沙箱里改不动工作区已存在的文件（`ModifyBackup failed: path index collision`），
# 只能把新版文件写到这里、再由用户手动移到位。属于工具链的临时产物，不入库。
/_patch/
.vite/
*.tsbuildinfo
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- Tauri 自动生成的 ACL / schema 快照 ----------
# 通配写法：每加一个 Tauri 应用就多一个 gen/schemas，逐个列举必然漏
# （探针 apps/webview-probe 就是这么漏掉的）。
**/src-tauri/gen/schemas/
**/gen/schemas/

# ---------- 运行期数据：审计库 / 证据截图 / 日志 ----------
# ★★ `/data/` 下的 `tasks/<任务ID>/` 是**真实界面截图**：截的是真机上的微信界面，
# 里面有联系人的**真实姓名**与聊天片段。它只用于本机排查（`docs/todo.md` T29/T30），
# 任何情况下都不许入库；现场收编成的用例（`data/cases/`）也在这里，同样永不入库。
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
/data/
/evidence/
/logs/
*.log

# ---------- 防手滑：现场素材不许进仓库 ----------
# 规矩写在文档里靠自觉，写在这儿是**机器执行**的。截图只应该出现在 /data/ 下，
# 谁要是把它拷进 fixtures 当测试素材，`git status` 里直接看不见它。
# ⚠️ 只拦图片：`tests/fixtures/` 下的 `events.jsonl` 是**人工造的假场景**，
#    那是要入库的回归用例（里面不该有真实姓名）。
**/fixtures/**/*.png
**/cases/

# `datasets/` 下是**真机微信 / 企业微信的截图**（`chat_ui_yolo_demo/images/` 与
# `crop_samples/` 的裁图里带着真实群名、姓名与聊天预览），与 `/data/` 同一条红线：
# 只在本机用，任何情况下都不许进版本库。
/datasets/

# mem-diag 每次跑都生成一份带时间戳的报告，不进版本库
tools/mem-diag-*.txt

# ---------- 环境变量与密钥 ----------
.env
.env.*
!.env.example

# ---------- 编辑器 / 系统垃圾 ----------
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ---------- 本地 AI 工具链数据（非项目源码）----------
.workbuddy-ai/
.workbuddy/
